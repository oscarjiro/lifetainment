$(document).ready(() => {
    // const distance = $(window).scrollTop();
    // if (distance > 10) {
    //     $("nav").attr("class", "preset-bg-pink");
    // } else {
    //     $("nav").attr("class", "preset-text-white");
    // }
    // $(window).scroll(() => {
    //     const distance = $(window).scrollTop();
    //     if (distance > 10) {
    //         $("nav").attr("class", "preset-bg-pink");
    //     } else {
    //         $("nav").attr("class", "preset-text-white");
    //     }
    // });
});
